# Template: Copy this file to `.env` and fill in values.

# The base URL prefix for API calls (e.g. /api when using the proxy below)
VITE_API_BASE_URL=

# Port for Vite server.
FRONTEND_PORT=

# The backend server URL for the Vite dev proxy (Flask API)
VITE_BACKEND_URL=